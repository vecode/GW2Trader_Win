<UserControl x:Class="GW2Trader.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid Width="Auto" Height="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Update Database" BorderBrush="{DynamicResource AccentColorBrush}" 
                    Style="{DynamicResource SquareButtonStyle}" BorderThickness="1" Margin="2"
                    CommandParameter="{Binding}" Command="{Binding Path=UpdateDatabaseCommand}"/>
            <Button Content="Rebuild Database" BorderBrush="{DynamicResource AccentColorBrush}" 
                    Style="{DynamicResource SquareButtonStyle}" BorderThickness="1" Margin="2"
                    IsEnabled="False"/>
        </StackPanel>
        
    </Grid>
</UserControl>
